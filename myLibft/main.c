#include <stdio.h>
#include <unistd.h>
#include "libft.h"

int main()
{
	char str[] = "FooBarFooBar";
	char dest[32];
	char *ret;

//	ret = ft_memchr(str, 'r', strlen(str));
//	ft_memcpy(str+5, str, strlen(str)+1);
//	ft_memset(str, '$', 5);
//	bzero(str, 3);
//	ft_memccpy(dest, str, 'r', 9);
	printf("%s\n", ret);
//	puts(dest);
	return (0);
}
